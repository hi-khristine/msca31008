Use this folder to store output files built with code.
These are usually data files, since data files shouldn't typically be tracked with Git.
Instead, you can run the code (which is tracked) to build these files.